apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak-jobs
  namespace: argocd
  labels:
    app.kubernetes.io/name: keycloak-jobs
spec:
  destination:
    namespace: keycloak
    server: "https://kubernetes.default.svc"
  source:
    repoURL: cnoe://keycloak-jobs
    targetRevision: HEAD
    path: "."
  project: default
  syncPolicy:
    automated:
      selfHeal: true
    syncOptions:
      - CreateNamespace=true